# computer vision and the concepts of layers

Good morning, it's time and we have a lot of stuff today, so let's get started once I of course do the quick Zoom sound check. Hello. Okay, so no response, let's, ah, there we go, yes, thank you. So let's do deep learning this week, finally. So we spent a week looking at some examples and you're supposed to set up some kind of working setup for yourself last week. So hopefully this works and then finally this week we get to test it. So we discussed a lot on deep learning and we looked at some examples and now we want to actually see how this works and start building our own models and the topic for now the next three weeks I think will be deep learning on image inputs. So we want to do something like this, take an image and then maybe find, well, a car, you could put a box around to mark the location if you want or you can have recognition of people and count them if you like or stuff that is relevant in maybe self-driving cars, realize this is a traffic light and then interpret what it's showing, in this case a red light and so on. So you can imagine that there's loads of difference, yeah, let's say use cases for computer vision, as we call it. These are some, but we'll start with the kind of simplest thing, which is classification. So just looking at an image and then say, is there a car in this image, yes or no? This kind of thing and then we'll move on in the next couple of weeks to either put these so-called bounding boxes for the location or even get a very fine mask around the object, which is image segmentation and this we will get back to. But then this will be the topic of the next couple of weeks, looking at basically image inputs and then, yeah, as we know, we'll move to text and time series and stuff later. But then for now, it's all about images. And then, well, you remember from last week we did this little thing of trying to make a, okay, let's select some networks. So we could make a neural network by adding these neurons in layers and then we can solve a task, a classification task in this case. Where we saw that for simple cases, we could use a simple network and difficult cases, we needed a more complicated or flexible type of model. And then we saw basically how each of these layers are doing transformations. So we have some data, but we transform it into different data and then we transform it even into more different data. And then the final result that comes out is something that is useful to solving this task. And then we had a clever question about, so when should we add more nodes and when should we add more layers? And when you're just on this kind of abstract data transformation level, it was a bit, I realized it was a bit difficult to explain. But once we go to these different layers that are useful for image classification, then we kind of see, we can visualize what each of them are doing and then kind of see that we extract different patterns from data. So we do pattern recognition, but when we look at images, we can actually kind of see what sort of patterns we are finding. Okay, so my little network solved this spiral task thingy, and that's nice. But then let's move to not just values, but for images we have, you know, many values and not just a list, but it looks rather different.

## Shallow learning (10:12)

But then, last week, we defined deep learning. One thing you can do is shallow learning. You have some features and they go directly to your model, and you make some predictions. Last fall, we worked on various types of models that do this. Additionally, from the example on the previous slide, we realized we could create our own features. This means sitting down and manually writing up some transformations that help with classification. These engineered features can then be added to our model, potentially improving its performance. If we were to express this as a function, our prediction \( Y \) would be based on our model \( F \), which is a mathematical function. It takes the first feature, the next one, and our engineered features, along with some parameters, and computes the prediction through typical operations such as multiplication and addition, possibly including if statements for decision trees. However, now we want to explore a more sequential approach where each feature feeds into the model sequentially, leading to a prediction.

## Deep learning (11:39)

Which makes it kind of obvious the structure that we will be using a lot for composing these networks, which are the layers where the first layer takes an input and then gives its output to the next one. Finally, we get the prediction, and the entire thing is kind of our model. If we spell it out as a function, it looks a bit different because now we have composite functions instead. So it's not just one function that takes a load of features in, but actually we have one function that takes the first features and its parameters, and then we take this into the next, which has its own parameters, and so on. This is a nice composite function we call it when we have a function applied to a function and so on. And we'll see for technical reasons that this is quite nice when we want to optimize the model. So this is deep learning for us. Transformation, transformation, transformation, and then we get something which is nice to predict upon. We don't have to do it once more, but you can, of course, run this yourselves. Let's quickly sum up the stuff I said. Still a bit abstract, but I will promise we'll get to the images very soon. If we have just our data, such as an image, for instance, you could imagine some image that you have. It could be anything. And then you want to decide, was there a cat in this image, for instance? Then if you just have the data and you have a simple rule to write, you could say, OK, if the upper leftmost pixel is green, then yes. It's kind of a silly rule, right, because just looking at the single pixel isn't really going to find a cat in an image, for instance. Typically, it's insufficient just to predict directly on your data. In some cases, it might work, but very usually not. Next, a better thing we can do, which is good for some data. In many cases, this is fine. I start with my data and then I have some kind of function. This transformation turns out to be better for predicting. And this is kind of what we saw. And then I can sort of see how I can keep doing this to improve it further. I can make one representation and then refine it and make a second one and predict, and it's even better. You can imagine putting more and more of these abstract representations in the middle and doing even better. And this is something we will do a lot. So you can try to keep this kind of in the back of your head so that for each layer that we introduce, we make some kind of representation and some kind of abstract space. But this is kind of better at separating different patterns. And this is the end goal, recognizing some patterns and then depending on the task, using these patterns for this or for that.

## The feed-forward neural network (15:57)

And then we had the demo, just a bit of repetition on how it looked. It was a linear regression, specifically multiple linear regression. However, we introduced an activation function, making it non-linear. This gives us the power and flexibility to create any type of complex function. Therefore, when dealing with data having a complicated distribution, we can now create a complex function that distinguishes between different categories for classification purposes. 

We still intend to perform this step at the end, as we aim for a single output—whether an image contains a cat or not. To achieve this, we need to preprocess the image data first. This involves adding numerous layers to transform the image into a form that is useful for our needs. While the original layer is used in all cases, it is typically applied only at the end to finalize the prediction.

## Image classification (17:21)

We move to the typical case of the first major computer vision project. Imagine it’s around 1990. You might be at home, listening to whatever was trending in the tech industry back then. This is roughly the era, right? In the first notebook, we have a task: we have 10,000 handwritten images—no, actually, 10,000 handwritten digits. Our goal is to automatically convert them into a digital format. We don’t want to manually read everything; we aim for simple optical character recognition (OCR) specifically for numbers. 

Each image consists of pixels, and they are grayscale. To classify each digit, we consider the numbers zero through nine. For each pixel, we gather information and decide whether it represents a number. If there’s a value, it means the pixel is part of a number. We can visualize this by focusing on a single pixel and seeing patterns. For example, a number two might have a small circle with a lot of surrounding area. If multiple pixels highlight similar areas, it suggests a number two. However, this method isn’t always accurate. If we shift the number two slightly, the classification changes. Additionally, if we change the size of the number two, it doesn’t align with our predefined pattern. Thus, we need an approach that is invariant under translation and dilation. A straightforward solution involves ensuring all number twos are centered and look consistent, which limits scalability. To overcome these issues, we need a better method.

## Enter the convolution operation (22:15)

We can do what's called convolution, and mathematically, it's an operator. It takes two functions and produces a new function. It looks somewhat complex. You don't need to remember this, but let's quickly see if we understand what it does. An integral involves a product of two functions, and we rearrange one while integrating. Essentially, we multiply two functions and integrate over them. It looks like this: \( \int F(x)G(x) dx \). Here, \( F \) is the red function and \( G \) is the blue function. We move one function around and integrate. The result is another function, which resembles a black line showing how similar the functions are. Initially, they don't match, but as they align, they show a perfect match. Then, they start matching again, but not perfectly. The convolution gives us a measure of similarity. For a different function, the alignment isn't perfect, but we can still find where they are most similar. Let's look at this again. At zero, they were most similar, but there's still some difference. The output from the convolution is another function, which looks like this. Although the equation might seem intimidating, the key point is that convolution allows us to recognize if shapes are similar and where the patterns occur. Our goal is pattern recognition. It seems like a perfect tool for this. Despite the integral, it works effectively.

## Discrete convolution (24:57)

But let's do it discretely because then it looks all much easier. Integral just becomes a sum, and summing numbers is easy. Instead of dealing with these functions, we actually have our image and a second image called a filter. Names aren't really decided on. You'll see both of these. For a picture, it looks something like this. So, read the book afterward, and chapter eight will give you a feel for what it does. But let's look at the first upper corner. My first function is an image, where you can see this as pixels, right. There are some values, and that's the pixel light intensity. If it's zero, there's nothing happening in this pixel. But if it's a high number, something is happening. That's how images work. My second function, which I call the filter, we need to do this. But it's now easier because we just multiply and sum. Always it's just multiplying and summing. So if I take this thing and put it over here, I'm supposed to multiply the numbers that match the pixels and then add them up. And then I get one number out, which is the value for this spot. Try to visualize this on your own. If I take like this pixel in my filter and it matches this place, I'm supposed to take minus one times three. Then I do the rest for all of the other positions and sum them up. The final result is minus three for this one. So we stick the output values in the center position of this. Then we move to the next spot. It's going to look more or less like this.

## Convolution over images (27:15)

In the orange area, I apply my so-called filter and drag it along all possible positions in the image to compute the outputs. Right. For one channel, it looks like this. If I had a grayscale or black and white image, it would look like this. I test all positions and record the output number. You'll notice that the output has fewer pixels than the input, which is due to the difference in size between the image and the filter. Additionally, I only test positions where the filter fits. However, I can adjust this to produce an output of a different size if needed. Essentially, we're testing all these positions to find patterns. As seen here, it's a four, which is a good match. A three is a bit less, and a two is even less, indicating a better match. Essentially, we're testing all possible positions to identify the pattern we're looking for. Pattern recognition and localization are key aspects. 

If we move to RGB images, it becomes more complex, as we need to consider red, green, and blue colors. The concept remains the same; we test all positions and sum everything at the end. I can choose to sum differently, but this is the typical approach using filters for each color channel and then summing them up to get the final output, which is an abstract space rather than red, green, and blue. There are various visualizations available online, such as YouTube videos, which can provide further insights. A visualization is also included in this week's to-do list. Testing all possible locations helps us find the pattern our filter is looking for. This method isn't entirely new, but it's a fundamental technique in image processing.

## The convolution kernel (or filter) (30:12)

I was claiming that in the 90s, we started doing this. However, the filter business has been done in digital image processing for a long time. The goal is to incorporate machine learning at some point. Before that, we had human learning, where people created various filters for different tasks in image processing. One nice aspect is that you can visually understand what these filters do. For example, if there are high numbers on a vertical line, the filter highlights vertical, diagonal, and horizontal lines. In the book, applying a filter that looks for a left-to-right diagonal results in high values where such diagonals appear. Here, the diagonal runs in the opposite direction. While there isn't much here, there is a slight indication. This area clearly shows a diagonal, and I can recognize the shape. My shape was quite simple, and imagining something more complex leads us to the next point. Keeping things simple often turns out to be beneficial.

## More filters (32:03)

If you're into photography, you've probably seen many filters already. For example, if I have one that averages everything, the result is just blurring. You can see how, yes. If I use a filter that involves all neighboring pixels equally, it will blur everything like this. This might be useful in some cases. Some filters are popular and have specific names. For instance, one finds vertical edges. Applying this filter to an image results in this output after convolution. Filters do stuff, essentially finding patterns. This is exactly what we need them to do. Filters have been around for ages, but now we want to learn them. Not all images we want to classify will have vertical lines; they might have other features. My computer needs to handle this.

## Kernels for image recognition (33:21)

Back to these numbers, a different number this time. Let's examine what appears to be a number five. I want to create a filter that matches this. Fine, so I’m not focusing on individual pixels anymore; instead, I’ll perform convolution. I make a nice filter. Then we observed that if filters look the same, they have a good match. So let’s create a filter that looks the same and apply it. And then I solve. Now, my filter is the same size as the image. There’s no room to drag it around within the image, but if I move it outside, it won’t provide any useful information. However, when it hits number five, it gives a result of 0.98, which is very close to a perfect match. The translation problem is solved. It turns out that using this filter and performing convolution results in a very good match. This has already improved things significantly. Now, I can move the number around and it will still be a very good match. But let’s test it against other things. For the number eight, there might still be a slight match, but we can accept this because they are somewhat similar. So I set my threshold at around 0.8. Any value above is considered a five. It wasn’t perfect, but it’s working nicely. However, there’s still an issue. If I write the number a little smaller, it doesn’t work as well. The translation process of moving things around is easier with convolution because it tests all possible spaces for where the pattern could be. But once I start changing the sizes, it’s not perfect to have a single large filter that finds everything. This was a very poor match. Therefore, we need to do something slightly better, such as using smaller filters.

## Decomposition into simple patters (36:00)

If I have these big filters supposed to capture the entire image, I need more or less as many filters as I have images of that size. This is not a very scalable approach. Instead, we can use smaller filters, such as three by three pixels, which is often the standard size. Five by five pixels might be used in some cases, but they are generally very small. Three by three pixels are typically too small to recognize the entire image, but they work due to the method we will explore. 

The question on Zoom was whether downsampling the image matters. The answer is yes, and we want to do this frequently. You are already ahead of us. Let's first examine what we get from these multiple small filters and then consider the magic of upscaling. However, if I have at least these three filters, an example would be needed. I want to highlight straight lines and edges in different directions. This will light up certain areas, allowing us to focus on specific features of the number. We also learned that applying this to all positions results in a slightly lower resolution image but highlights the features identified. This makes conceptual sense, doesn’t it?

To identify the number five, we need a flat top, a downward bit, a 90-degree edge, and a semi-circle at the bottom. The number five is truly described by these small patterns. By examining these patterns, we can determine where they appear and combine them to form larger shapes. This process can be repeated with different patterns. 

Looking at a simple image from the book, it’s about decomposition. If there’s a large-scale pattern like a cat’s face, it’s made up of smaller lines. By assembling these generic lines, we can create an eye, nose, and ear. Repeating this process with two eyes, a nose, and an ear can classify it as a cat. Notably, the lines used to identify an ear are not the only ones involved; different lines contribute to this classification. This approach drops the idea of a single mega filter recognizing everything. Instead, we use small filters to identify generic lines, which can be combined into larger patterns.

This decomposition makes the process efficient. In the first layer, filters identify basic elements. These can be combined to form various shapes, leading to the desired higher-level pattern, such as a cat. This hierarchical representation allows for efficient recognition of complex patterns using smaller, reusable filters.

## Keras layers (41:36)

This brings us to the Keras library. Before we discuss everything, let's have a quick look at the web page itself. The API docs provide descriptions of all the layers, and you can also find guides, examples, and how-to sections. Some of the notebooks come from these examples. Let's go to the documentation and see what different layers we have to work with.

## Keras 3 API documentation (42:18)

You'll notice that there's a lot of stuff, so we don't need to know everything kind of right up. But afterwards, you will get familiar with much of this. Layers are where these building blocks live, and there are different ones, many different options, as you'll see. For instance, this one. We want to do convolution, and the library is very helpful in this, providing different types of convolution layers. Some of these are less commonly used, but the first ones are used very often. The name is basically just conv for convolution, and then you have to select how many dimensions you have. A picture is like X and Y, right? So I have two dimensions. So I choose this layer, and then I can read everything about this. This is a common layer with lots of options for different stuff. I just want to quickly mention some of these for you today before we end and continue tomorrow. One layer does its thing, and then you can move on to the next, just like we saw. When operating on images, we need different types. We want to do convolution, and this was the business we basically saw. All of this was convolution, and we had these filters, and they did stuff. You can see, okay, this is a transformation, right? I get kind of similar but something else out. I can't make a prediction directly on this, but it's better for a prediction. Convolution layers do the convolution step. Not surprisingly. But then I also mentioned that downsampling might be something to gain, and there are layers for this, which will be covered tomorrow. These are helpful. Finally, we need the original fully connected layer, the dense layer, to make the final prediction. Because then we have all our features coming out, but we still want to do the aggregation into one single prediction. So then we're going to need this original, the one layer that we know we need at the end. But then we use all these different layers that we just learned to get this feature extraction bit. So I think we have five minutes left.

## The Conv2D layer (45:27)

Let's do the two-dimensional convolution layer next. Then we might finish early since I have a meeting. We'll pick up where we left off tomorrow at 10 and build our final model. Alternatively, you can test it in the lab today. However, the documentation lists several arguments, but we only need the first two. There's flexibility, but we'll start with these two things. First, we need to decide how many small patterns to look for. Large computer vision models can range from tens to thousands, depending on the optimization. This is something we'll determine through testing. Second, we need to decide the size of each pattern. Smaller patterns are often more effective, especially when downsampling. We'll see this in action later. These are the key decisions. Typically, we'll use a three by three filter, but you can adjust based on more complex needs. Testing is crucial. Finally, I placed the filter in all possible positions, resulting in fewer output pixels. Alternatively, you can place the filter outside the image to maintain the same output size. These are some options. We'll resume this discussion tomorrow at 10. I wanted to show you one of the notebooks. Clicking this link on Canvas will take you to the notebooks for this week. You can view them or run them in Colab. Let's set up a virtual machine for tutorials and exercises. We'll have some interactive sessions and hands-on tasks. This will help you understand the concepts better. We'll meet at the lab in two hours to solve the classification task. That concludes today's session. We'll meet again both in the lab and tomorrow.
