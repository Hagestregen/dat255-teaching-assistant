# computer vision and the concepts of layers
Good morning, it's time and we have a lot of stuff today, so let's get started once I of course do the quick Zoom sound check. Hello. Okay, so no response, let's, ah, there we go, yes, thank you. So let's do deep learning this week, finally. So we spent a week looking at some examples and you're supposed to set up some kind of working setup for yourself last week. So hopefully this works and then finally this week we get to test it. So we discussed a lot on deep learning and we looked at some examples and now we want to actually see how this works and start building our own models and the topic for now the next three weeks I think will be deep learning on image inputs. So we want to do something like this, take an image and then maybe find, well, a car, you could put a box around to mark the location if you want or you can have recognition of people and count them if you like or stuff that is relevant in maybe self-driving cars, realize this is a traffic light and then interpret what it's showing, in this case a red light and so on. So you can imagine that there's loads of difference, yeah, let's say use cases for computer vision, as we call it. These are some, but we'll start with the kind of simplest thing, which is classification. So just looking at an image and then say, is there a car in this image, yes or no? This kind of thing and then we'll move on in the next couple of weeks to either put these so-called bounding boxes for the location or even get a very fine mask around the object, which is image segmentation and this we will get back to. But then this will be the topic of the next couple of weeks, looking at basically image inputs and then, yeah, as we know, we'll move to text and time series and stuff later. But then for now, it's all about images. And then, well, you remember from last week we did this little thing of trying to make a, okay, let's select some networks. So we could make a neural network by adding these neurons in layers and then we can solve a task, a classification task in this case. Where we saw that for simple cases, we could use a simple network and difficult cases, we needed a more complicated or flexible type of model. And then we saw basically how each of these layers are doing transformations. So we have some data, but we transform it into different data and then we transform it even into more different data. And then the final result that comes out is something that is useful to solving this task. And then we had a clever question about, so when should we add more nodes and when should we add more layers? And when you're just on this kind of abstract data transformation level, it was a bit, I realized it was a bit difficult to explain. But once we go to these different layers that are useful for image classification, then we kind of see, we can visualize what each of them are doing and then kind of see that we extract different patterns from data. So we do pattern recognition, but when we look at images, we can actually kind of see what sort of patterns we are finding. Okay, so my little network solved this spiral task thingy, and that's nice. But then let's move to not just values, but for images we have, you know, many values and not just a list, but it looks rather different.

## Shallow learning (10:12)

But then first, so we also, last week, sort of defined deep learning. So one thing you can do is shallow learning, right? So you have some features and then they go directly to your model and then you make some prediction. So, yeah, last fall we did a lot of different types of models that do this. And then we also realized from the example on the previous slide that I can also make some features myself. I can sit down and kind of manually write up some stuff that makes or does some transformation that makes it easier for me to classify. So I can put this into my model and then improve things. And if we want to write it up as a function, my prediction Y is going to be my model F, which is some kind of mathematical function, and then it takes in the first feature and the next one, and then also my engineered features and at the end some parameters, and then I compute my prediction by typically multiplying and adding numbers and maybe some if statements if I have a decision tree and so on. But then kind of all the features go into the model and then comes a prediction. But now we want to do something like this and we see this kind of sequential nature of it,

## Deep learning (11:39)

which makes it kind of obvious the structure that we will be using a lot for composing these networks, which are the layers where the one first layer takes an input and then gives its output to the next one. And then finally we get the prediction and the entire thing is kind of our model. And then if we spell it out as a function, it looks a bit different because now we have all these composite functions instead. So it's not just one function that takes a load of features in, but actually we have one function that takes the first features and its parameters, and then we take this into the next, which has its parameter and this to the next and so on. And it's a nice, well, a composite function we call it when we have a function applied to a function and so on. And we'll see for technical reasons that this is quite nice when we want to optimize the model. So this is deep learning for us. So transformation, transformation, transformation, and then we get something which is nice to predict upon. We don't have to do it once more, but you can, of course, run this yourselves. So let's quickly sum up the stuff I said. So still a bit abstract, but I will promise we'll get to the images very soon. But if we have just our data, so if we have, yeah, we can take an image, for instance. So we can imagine some image that you have. It could be anything. And then you want to decide, was there a cat in this image, for instance? Then if you just have the data and you have kind of one if sentence to write, you could say, OK, if the upper left most pixel is green, then yes. And it's kind of it's a silly rule, right, because just looking at the single pixel isn't really going to find a cat in an image, for instance. So typically it's insufficient just to predict directly on your data. In some cases, it might work, but very usually not. And then you can think really hard about this while I try to fix the fact that Zoom is crashing. So there I lost my slides. Let's try and connect again, maybe like so. So next thing, a better thing we can do, which is good for some data. In many cases, this is fine. I start with my data and then I have some kind of function. This transformation turns out this is better to predict upon. And this is kind of what we saw. And then I can sort of see how I can keep doing this so I can do even better. I can make one representation and then refine it and make a second one and then predict and it's even better and so on. And you kind of can imagine that I can put more and more of these abstract representations in the middle and then do even better. And this is something will will do a lot. So you can try to keep this kind of in the back of your head so that for each layer that we introduce, we make some kind of representation and some kind of abstract funny space. But this is kind of better at separating different patterns. And this is the in the end what we want to do want to recognize some pattern and then depending on the task, we use these patterns for this or for that.

## The feed-forward neural network (15:57)

And then we had this thing for the demo, right? Just a little bit of repetition how this looked. So it was kind of a linear regression thing, multiple linear regression. But actually, we introduced a activation function and then it's not linear anymore. And now we have the power and the flexibility to make any kind of funny looking function. So if data has a complicated distribution, now we are able to make make a complicated function that separates between things in the case want to do classification. So now we want to add some stuff in front of this. So we still want to do this in the end because we want one output, which is kind of OK, let's say a cat in the image. Yes or no. So we need to do this at least once. But want to add a lot of stuff in front of this, which is actually transforming the picture into some representation that is that is useful for us. So that means new new layers. So this was kind of the original one and we use it in all cases. But usually only at the end, as we'll see to make the final final prediction.

## Image classification (17:21)

So then we go to the kind of archetypical case, the first big computer vision thing. So imagine it's it's 1990 more or less. You can picture yourself sitting at home and listening to. I don't know what was the big thing in this business person stuff. So this is around the age, right? And this will will also do in the first notebook. And we have a task at hand, which is we have some 10,000 handwritten images, no, sorry, handwritten digits, images of these handwritten numbers. And then we want to automatically translate in to the kind of digital representation. So we want to whenever people send stuff in the mail, for instance, we want to process it automatically. We want to just recognize these handwritten numbers and see what kind of numbers numbers they actually are and want to do it on a machine. And we don't really want to sit and have all the manual work of reading it. We just want to do kind of simple optical character recognition. That's as it's called now, OCR, but for numbers. So we have this thing, right? And there's a couple of pixels. You can see it's not very high resolutions. We see each pixel and it's a grayscale thing. And then how do we go about to classify this as a number? So we have the numbers are zero to nine, right? So we have 10 different options for what this could be. And then we want to take each pixel and then somehow aggregate all this info and then get decision number two out. OK, so what we learned was that we have this thing which take features in. So the kind of first thing that you could do if you know about this type of layer is to say, OK, X one is pixel number one. X two is pixel number two and so on. These are values and it's zero if it's white. Yeah, I guess some number if it's if it's black, maybe it goes from zero to one, for instance. So then, OK, if I do this approach, so kind of very roughly says if I take this as a pixel and this is a pixel and this is a pixel and so on, just to kind of show you how it more or less would be would be done without taking all the pixels. We kind of imagine that's right. OK, so there's some kind of pattern going on for this number where there's a little bit of stuff in this circle, a lot of stuff in this circle and then kind of going on. And I could do this for all the pixels of just make the circle smaller. And I kind of see that, OK, this is the pixel representation of this number two. OK, kind of makes sense, right? So if there's some value in these pixels and all these pixels, then this in total should mean that I have number two. And if there are different pixels highlighted, then it should be a different number. So this is kind of the immediate thing that we could do and then kind of see pretty quickly that doesn't always work that well. So if I, instead of centering my number two in the middle, I maybe have a little smaller number two and I put it down in the corner and then I didn't really line up with these colored circles that I had identified as my pattern. So you have two kind of immediate problems here. So first, we said it's not invariant under translation, just meaning if I move stuff around in the image, then I get different results. So now my number two is no longer where it was supposed to be. But I can still see that this is a number two, right? But my simple neural network counts. And then second thing, also not invariant under what we call dilation, so just changing the size. It's a bit smaller now and then it didn't really fit up with what I kind of handcrafted as my pattern for this number two. So just one pixel is one input, didn't really work that well. It was a nice idea, but then I have to ensure that all my numbers two are in the middle and they look kind of the same and it doesn't really scale all that well. So this is something we have to solve before we can really move on in computer vision. And what can we do? Okay, so we can do one kind of fancy thing, which turns out to be quite simple.

## Enter the convolution operation (22:15)

So we can do what's called convolution, and it's mathematically speaking, it's an operator. So it takes two functions in and gives you a new function out. Looks a bit funny. You don't have to remember this, but let's just quickly see if we understand what it does. So there's an integral over something. We have a product of two functions and we kind of move this one around and then integrate. So multiply two functions while trying to move the one function around and then integrate over everything that comes out. It looks more like like this. So if we have now my F is the red thingy, no, sorry, blue, and the other function is the red. They are kind of two boxes and they look the same. So we're basically moving the one around, right? And then integrating over all of it and basically see how well they match. So now they don't match. Now they don't match. There they start matching. There's a perfect match and then it's kind of matching and that is not matching anymore. So kind of see how basically this well black line shows to what extent are these functions similar. How they're identical. Now they are a bit less similar. So it gives you a measure. OK, are they similar? And also, if so, where exactly are they the most similar? I can do it for a different function like this one. So now I still have my red square thingy, but then like sort of different looking blue thing. So they're not identical. So I don't have a perfect score anymore, but I can still find where they sort of looked the most the same. So let's look at this again. So zero, zero, zero, zero. Here's something's happening. This is the point where they were most similar and then I kind of there's still something going on, but a lot smaller value. And the thing I get out from my convolution is another function. This thing that goes like here. And although so the equation looks a bit looks a bit intimidating, maybe. But the point is just we have now one operation that allows us to recognize if shapes are similar and where exactly in data did we find the pattern. And our goal in the end was to do pattern recognition. Right. So seems this is kind of the perfect thing to do. Then it looks a bit iffy with this integral and stuff.

## Discrete convolution (24:57)

But let's do it discrete because then it looks all so much easier because integral just becomes a sum. Summing numbers is easy. And then instead of these functions, we actually have our image and then a second image that we'll call a filter typically or kernel. People can't really decide on the names. So you'll see see both of these. And then in for a picture, it looks something like like this. So, of course, read the book afterwards and chapter eight is it to kind of get a feel for what it does. But let's try and try and look. So I have more examples in the next slide. But now let's look at kind of this first upper corner. So if I was saying so my first function was an image so you can see this as pixels, right. And I have some values and that's the pixel light pixel intensity is what it's called. So if it's zero, there's nothing going on in this pixel. But if it's a high number, then you have something going on. This kind of our images work. Right. And then I have my second function, which I call the filter. And then I want to do this. But I said it's now easier because I just multiply and sum. So always it's just multiplying and summing. So if I take down this thing, kind of put it over there and then I'm supposed to multiply the numbers that add up. No, sorry. Multiply the numbers that match the pixels and then add up. And then I get one number out and that's kind of the value for this spot. It's OK. Also try to visualize this on your own. But if I take like so this pixel in my filter matches to this place, right. So I'm supposed to take minus one times three. So that's this thing. And then I do the rest for all of the other positions. And then I sum up and then turns out the final result is minus three for this one. So this is what we do. And then we stick the output values in the center position of this. And then we move to the next spot. So it's going to look more or less like this.

## Convolution over images (27:15)

So have in the orange is kind of where I'm applying my so-called filter and then just drag it along all possible positions in the image to compute the outputs. Right. So have for one channel, it looks like this. So if I had a grayscale or black and white image, it would look like this. And then I just keep testing all positions and I write down the output number. And then you'll notice, OK, so the output has fewer pixels than the inputs, which was because there was a difference between the size of my image and my filter. And also I'm only testing kind of the positions where my filter actually fits. But this I can choose to do differently if I want a differently sized output. But this kind of thing. So yeah, test for patterns basically in all possible positions. And then we see. So this was a four. So it's pretty good match. This was a three. So a bit less. This was a two. So even less for is better match and so on. So what are we getting out? It's basically testing all these positions. Did we see the pattern that we are looking for? So again, pattern recognition and also pattern localization. Very nice seems to be the stuff that we need to solve our kind of kind of problems. OK, if you bump it up to RGB images, it gets a little more complicated, right? Because now you have to do this in all different colors. So the red, the green and the blue colors. But the concept is kind of kind of same. So you test for all positions and then you just sum everything in the end. So the output is just this one because I sum for all my different colors. Again, I can choose to do it differently. But this is the typical, typical approach that I have different filters for different channels. But then in the end, I just sum everything up and then I have my output, which is no longer red, green and blue. It's just abstract space, more or less my convolution outputs. There's well, there are loads of different visualizations for this. So also just please Google it and look at a YouTube video in case you in case you want to know. So even provided one for you on the canvas if you have a look at this week's to do stuff. So this is how it works. Test all possible locations. Did we see the pattern that our kind of filter, so to say, is looking for? And then, well, this business is not very new.

## The convolution kernel (or filter) (30:12)

So I was claiming that kind of in the 90s, we started doing this. But actually, the filter business we have done for a long time in digital image processing. So the thing is just to try to at some point involve machine learning. But before this, we had kind of human learning. So people made all kind of filters to do different stuff in image processing. And the nice thing with this is you kind of can just look at it and see to some extent what it's doing. So if I have one thing which has high numbers on a vertical line, then basically this is going to highlight vertical lines and diagonals and horizontals and so on. And we can kind of see this in one of the examples in the book that if I have, on my letter, no, sorry, number zero in this case, and I apply a filter which looks for this left to right diagonal thing, then the output is going to get a high value where I have this kind of diagonal. So here, for instance, the diagonal goes the opposite direction. So I don't see much, a little bit, but not that much. But this place, I have the diagonal and I can see it. And also in this place, I can sort of recognize that this is where I saw this type of shape. And my shape was super simple. And we can imagine that I maybe want something more advanced. And this will also get to, but actually keeping it simple turns out to be to be a nice thing. So even one more digression on this.

## More filters (32:03)

So if you're into photography, you've probably seen a lot of filters already. Like if I have just one that highlights everything and takes an average, in the end it's just blurring things. You can see how, yeah. Okay, if I have this thing and then I keep involving all the neighboring pixels to the same extent, then it's just going to blur it out like this. Okay, so this you might want in some cases. Some filters are popular and they got their own name. Like this one, it's basically finding vertical edges. So if I have this thing and I apply the filter, this is what I get out after my convolution operation. So you can see these filters do stuff, right? And they find patterns essentially. And this is again exactly what we need them to do. So that said, so filters we had for ages, but now we want to start learning these filters instead. So it's not guaranteed that all the images I want to classify have, for instance, vertical lines. Maybe they have something else. And this I want, of course, my computer to solve.

## Kernels for image recognition (33:21)

So back to these numbers, different number this time. Let's look at what seems to be a number five. And then I want to make a filter that matches this thing. And okay, fine. So I'm not looking at the individual pixels now. As I was in the first network, but I want to do convolution. So I make a nice filter. And then we kind of saw that, okay, filters, if they look the same, then they have a good match. So let's make a filter that looks the same and then apply it. And then I solve. So okay, now my filter is the same size as the image. So there's not kind of room for me to drag it around. But if I took this filter and then moved it around outside of the image, it would give nothing. But once I hit number five, it would give, in my case, computed it to 0.98. So pretty close to perfect match. So the translation thing I can solve. So turns out if I have this filter and do convolution, then it's a very nice match. So already I improved quite a bit. So now I can move my number around and it will still be a very good match. But okay, let's try to test it against other things. So for this number eight thing, maybe still a bit of a match. But then, okay, yeah, we sort of accept this because these are similar looking things to some extent. So I can say that I put my threshold at, I don't know, 0.8. Anything above is a five. So it wasn't perfect, but kind of working nicely. But then we have the one thing still. So if I take my number and I just write it a little bit smaller. It's the exact same thing, but it's a bit smaller now. Okay, now it didn't really add up that well. So this translation thing of moving stuff around, this was kind of easy with the convolution operation because I test all possible spaces for where the pattern could be. But once I start moving the sizes around a little bit, it's not perfect anymore to have this one big filter that's supposed to find everything. So this was really, really poor match. So we need to do something slightly, slightly better. And that brings us to the thing with having small filters.

## Decomposition into simple patters (36:00)

So if I have these big ones that are supposed to capture the entire thing, then I need more or less as many filters as I have images of this number. And it's not really a very scalable approach. But what I can do and what we want to do is to take rather just small filters. So three by three pixels is kind of just become the standard for how big a filter should be. Some cases may be five by five pixels, but still it's usually very small. So three by three pixels is typically way too small for you to kind of actually recognize the big image, but it works because of what we'll see. So the question for everyone on Zoom was does downscaling the image matter? The answer is yes. We want to do this a lot. So you're kind of ahead of us already. So let's first just look at what we get from these multiple small filters and then involve the magic of up and downscaling essentially. But if I now have at least these three ones, could be an example. So I want to have some straight lines and I want to have some edges and a different direction edge. This is going to light up here and that's going to light up there. And not other places, but we can at least highlight features of this number. And then we do this. So we also learned that this will, when I apply it to all the positions, it will downscale things by the size of my kernel filter. So I get a slightly lower resolution image, but it kind of highlights where I saw some of these features. This kind of makes conceptual sense, doesn't it? So if I learn how to write the number five, then what does it need? Well, it needs a flat bit at the top and then it needs a kind of bit going down and then it needs this 90 degree edge and then it needs a kind of semi circle at the bottom. So the number five is kind of truly, if you were to describe it, it is truly described by these different small patterns. So actually identifying these small patterns is quite nice. And then I see where did some of these patterns appear. And then so technically I get many of these for each pattern. And then I can look at this again with a new pattern and so on and repeat. And it's going to be something like this. So for a silly image taken from the book, it's all about decomposition in a sense. So if I have one kind of large scale pattern, which is the face of a cat, then well, it turns out it's actually composed of many of these smaller lines. So if I take all of these smaller lines that are kind of generic, I can apply or assemble them into an eye or different ones, assembly into nose, different ones, assembly into ear. And then kind of if I assemble this again, if I have two eyes and a nose and an ear, two ears, I mean, then this is likely to be a cat, right? Then one magic thing that I also noticed from this diagram is that so these lines that were kind of meant to identify an ear were not the only ones that went into classifying this ear. Actually, it was some different lines as well that went into this one. And that is kind of nice because now, so we dropped the idea of having one big mega filter that is supposed to recognize everything. Rather, we just have small filters that identify very generic lines. But then it turns out different compositions of very generic shapes or patterns, they make bits of bigger pattern, and that this, again, goes into the much or the kind of top level pattern that we're looking for, in this case, a cat. So it turns out now this is actually quite efficient approach because now I have different filters, they pick out different things. But these filters can be reused for different concepts. So these filters are both used to identify eyes and also identify ears. So I actually have a kind of condensed type of way of representing shapes that, again, can put up into a hierarchy and then get the final thing. So this decomposition makes it very efficient for me. So if I have like the first layer, it has just some filters that identifies completely basic stuff, then this can be composed into different things that give me the ability to get to the end goal of this high level pattern, which was composed of smaller things. And that's great. So we got us this far. Now we also want to have a look at how this works,

## Keras layers (41:36)

and this brings us to the Keras library. So I can actually, before we discuss everything, let's actually just have a quick look at the web page itself for the library. So this web page, I think you'll visit quite a lot. API docs has the descriptions of all the layers. And while you can also see guides, how to do stuff, examples, how to do stuff. Yeah, at least some of the notebooks will do comes from the list of these very nice examples. But let's go to the documentation and kind of see what different layers we have to play with.

## Keras 3 API documentation (42:18)

So you'll notice that, well, there's loads of stuff, so we don't need to know everything kind of right up. But afterwards, you will get familiar with much of these. So layers is kind of where these building blocks live. And there are different ones, many different options, as you'll see. But we can notice, for instance, this one. So I said we want to do convolution, and then the library is very helpful in this, providing different types of convolution layers. So some of these are a bit more, yeah, not that often used, but these first ones are used very often. The name is basically just conv for convolution, and then you have to select from how many dimensions do I have. And a picture is like X and Y, right? So I have two dimensions. So I choose this layer, and then I can read everything about this. And this is a common layer, has lots of options for different stuff. I just want to quickly mention some of these for you today before we end and continue tomorrow. So the first kind of basic building blocks of neural networks in the Keras library are these layers. So one layer does its thing, its transformation, and then you can move on to the next and so on, just like we saw. And then operating on images, then we need different types. So we want to do convolution, and this was the business that we basically saw. All of this was convolution, and we had these filters, and they did stuff. And you can see, okay, this is a transformation, right? I get kind of similar but something else out. I can't make a prediction directly on this, but it's better for a prediction. So convolution layers do the convolution step. Not surprisingly. But then I also mentioned that was maybe something to gain by downsampling, and there's also layers for this, and this will have them to look at tomorrow. But these are helpful. And then finally, finally, at the end, we need the original fully connected layer, the dense layer, as it's called, to make the final prediction. Because then we have all our features coming out, but then we still want to do the aggregation into one single prediction. So then we're going to need this original, the one layer that we know we need at the end. But then we use all these different layers that we just learned to get this feature extraction bit. So I think, okay, so we have five minutes left.

## The Conv2D layer (45:27)

So let's just do the two-dimensional convolution layer then. And then we can end, hopefully, two minutes early because I have to run to a meeting. And then we pick everything of this up again tomorrow at 10, and then build our final model. Or well, you kind of get to test it already in the lab today. But we saw in the documentation, so there was a long list of arguments to this one. Turns out we actually just need the first two, really. But let's say there's a lot of flexibility that we can use if we like. But the first thing, the one thing that we have to choose, the one thing that is, or the two things that don't have default arguments, are the following. So there's filters. And this was a thing where we kind of couldn't really decide, do we call it a filter? Do we call it a kernel? Well, it's the same thing. Okay, for some reason we can't really agree, but that's how it is. But the first thing we have to do is just say, okay, how many small patterns do I want to look for? And big computer vision models can range anything from some tens into some hundreds, maybe a thousand. And that's a value for you to optimize. So this we have to figure out by testing. And that's just how many small patterns am I trying to find in a sense. And the second one, okay, so I want all these patterns, but how big of a pattern should I look for? And how we mentioned that actually nearly as small as possible is typically a good thing. So why does it work to have these incredibly small patterns when we're looking for something big, like an image of a cat? The cat probably fills the entire image, and then we're looking for these kind of tiny matrices of pixels. Well, this we'll see that when we start downsampling and kind of removing unnecessary information, then we get to three by three in a downsampled image. It's actually kind of a big portion of the entire image in the end, as we will see. And these are the two things we need to say. So typically you're going to say three by three, and then you're kind of good, and then that was it. And the rest you can tune once you get to more advanced needs. For instance, you can imagine that instead of moving the filter at every step, maybe I want to move it only at every other step. Maybe that's better. I have to test. So lots of testing for you to do. Final bit, so I mentioned I put my filter in all possible spaces, and then I end up having fewer pixels in the output. But I can also kind of just move my filter outside of the image if I want the output here to be the same pixel dimension as the image. Maybe I want this in some cases. So these are some options that we have, and that brings us pretty close to the end. So we have to pick this up again tomorrow at 10, as mentioned. But one thing I wanted to maybe show you was one of the notebooks. So if you click on this link on Canvas to the notebooks, then you see we have three now, which is for this week. You can click this link just to see what it looks like. That's nice. But if you want to run it, so either you download it and run it on your own computer, or you click this Open in Colab, and then you can just do it in the browser, like I have here, and then it's super simple. You just start clicking and then go through it. And then the plan was for us to have some tutorials, which is kind of clicking through, and then you're supposed to read and understand, and then some exercises where less stuff is given, and you have to figure it out yourself, more or less. Okay, so this will set up a virtual machine for us. It takes slightly longer. Ah, there it goes. I can start running, downloading some samples, looking at numbers. So this will be the numbers that we tried to classify, and this will be the task for the lab today at 12. So two hours from now, let's meet at the lab and then solve this. So then we're out of time, so let's end it there and then meet again. Both on the lab and tomorrow.
