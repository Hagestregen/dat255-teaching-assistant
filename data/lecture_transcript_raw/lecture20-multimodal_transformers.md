# multimodal transformers
Hello, so Tuesday today, last lecture before the little Easter break, we have the chance to work a lot on a project hopefully, so let's do a bit more of the more kind of advanced stuff on transformers, I want to show also how you can use transformers for completely different things, so we have of course text and text generation being the big thing that we use the transformer architecture for and where it was originally also first used to great success, but turns out with some tricks up your sleeve you can use it for basically any data type, so it is a very versatile network architecture, so it's going to be very useful to us, but also requires a little bit of thinking to get going with different data types, since for text it's kind of obvious to us what a token would be, so just be a part of a word or a complete word, but if you start doing other data types then we might have to do some kind of consideration to what we would like to call a token that we can operate on. We'll get first, so yeah, yesterday I talked a little bit about training and Tarja also

## Training LLMs (1:48)

talked a little bit about training, that we have the self-supervised pre-training parts, so if you have heard about JetGPT you might know that GPT is for generalised pre-trained transformer, so the P is pre-training, but then we also have the fine-tuning bits, which makes it an actually useful thing for us. Okay, some data we discussed, distributed training I think

## Distributed training (2:21)

we discussed, so whenever you have a big model you need to usually do something, do a bit of engineering to make it fit on your hardware, so not super relevant to us unless you have a very ambitious project, but typically in our case we're satisfied if it fits on a single machine, but there are cases of course where it does not, so large language models they have the name for a reason, so they are truly large and that means you have to do a lot of engineering to to put data and to put your parameters on different computers or different compute nodes, so it's tricky but there are some tools for us in case we need,

## Supervised fine-tuning (3:12)

but let's look a little bit again on the supervised fine-tuning parts, so as we mentioned yesterday, so if you have a pre-trained model it has been through this autoregressive training loop where you mask off the end of a sentence and then you try to predict the end of the sentence and then you reveal and then you move on to the next word or next token, but that means you train a model to predict the next words, so the only thing it really can do is to predict the next words, which is well of course super useful but it's not useful for a question and answering setting, so if you then ask this pre-trained model a question that will just keep adding to your question and never really getting to the answer, but we usually want to get to the answer, so here comes the supervised part where we basically say okay instruction training for instance is what we call it when you want the model to basically follow instructions, so you can say for instance as an example translate this or that into a different language and then you provide the solution in this format that you have instruction and solution and then it's kind of built into the assumption that whenever the model is fed an instruction and then something is supposed to provide the solution, so in order to do this we of course then need to have actual solutions, so then supervised so we need to provide what is the answer to this and then the model is trained to come up with the solution, so this is then costly not in terms of computing that much, but it's very costly in terms of actually coming up with the training data, so if you're starting from scratch some human has to provide the solution to the instruction so that we can train on it, and here already you can sort of guess that there are ways to do this without explicitly for us sitting down and writing instructions and solutions because there are certain web pages that have this as their main thing, think of stack overflow for instance where people post an answer and then other people give instructions, so stack overflow and the like is of course very popular places to get hold of such training data, you can just scrape it and then get rid of all the html and css stuff and just have questions and answers, of course if you've ever been to stack overflow well or qora or these things you know that okay some answers are useless and some answers are good, so curating your training data so you have actually instructions with good solutions to them is a lot of work, so there are many elements to training a language model, so first you have to do the big or first do all the data scraping and then you have to do the big supervised no sorry self-supervised pre-training which takes a long time and then you're not done, you're kind of only halfway or 75 percent there, you still need to get hold of some nice data for supervised fine tuning parts. Okay so given that we have this then we are onto something and this step of obtaining this data is also among the important business secrets of the big AI companies, so we have to say that one of the great successes of open AI is well first to make make a nice architecture for the model but also doing this part with high quality was a great success of open AI, so one thing people have understood is that okay deep learning used to be all about having a lot of data right but turns out that when it comes to the supervised fine tuning bit this of actually making a good chat bot then quality is actually more important than quantity, so if you have instructions with very good clear and precise solutions then this is going to be better than having loads of medium quality stuff, so again this makes it important for all the big AI companies to curate and collect a very good list of such data sets to train on for this part. But then if you want to do the same thing or if you want to start from a pre-trained model and you go to hugging phase that we had a look at several times you'll notice that there are often two variants of the same model, the one is just the model itself so one popular one is from meta research called llama you can download this and that this one is just going to be the pre-trained thing so here you have to do the remaining fine tuning yourself it's just a word predictor it's very good word predictor definitely but it doesn't it doesn't answer questions but usually you will find the same thing with instructs appended to it which means it has been trained on this or gone through this instruction training so now you can use it for questions and answers. So depending of course on what you want you can choose from these if you just want a model that you can set up for your chatbot then instruct is going to be the one that you want. So if you look again on hugging phase hub there are loads of these and we mentioned also that well in addition to the instructs trained ones you will find different we called it adapters and we called it fine tunes and we called it yeah there's one more I think as well which are then basically the base model but adapted to do different things so since yeah again since it's so much compute that goes into the pre-training step we want to use this pre-trained general model for anything it's it's worth really.

## LLM fine-tuning with limited resources (10:03)

So fine tuning usually if we want to fine tune we don't have access to a massive data center and unlimited resources and all this is this good stuff but there are then a lot of things you can do with also limited resources so taking the full model that has been pre-trained and then doing a complete retraining of it usually not that relevant for us for two reasons so one obvious one is that well you have to buy a data center or at least rent it in order to get enough memory to stuff the entire thing into your GPU cards and we can well I think the book also mentioned it if you want to do this full retraining you have the you have the model itself that has to go into memory but you also need the optimizers numbers to go into memory so we were using typically adam right it was adaptive and it has memory and these things and that also takes quite a bit of memory the the optimizer itself so full fine tuning it's expensive but there's also this thing that people discovered after a while which called catastrophic forgetting which is basically basically what it says that if you take the full model and you try to fine tune it occasionally you will see that it actually forgets everything from the pre-training it kind of forgets everything it's learned in the past and now it can only do these very specific tasks that that you trained it to so now it's no at this point and there's no kind of foundation model anymore it's just a super specific thing to your data set and it's going to be overkill and it's probably not exactly what you what you wanted so retuning all the parameters of a big model are usually something you want to avoid for for these two reasons so then let's get into some techniques that we could we could use if we want to fine tune to a certain case so again this could be any business idea of yours maybe you want to make a chat bot for your company or no customer services or something which is quite popular or you want to do i don't know automatic documentation of your code base maybe you want to train it specifically on your code or or anything like this you want to just input your data somehow but still retain the capabilities of the of the original model so you can do the following thing so a couple of tricks i will look at today so one thing you could do so-called prompt tuning so in fact you can make a little model and then just stick it in front of the of the large language model so you do the tokenization bits but then you actually fiddle with the embedding space before it goes into the large language model so you can tune it to to your context so to say so this model will then output tokens into an embedding space but this embedding space is kind of yours you designed it and then the all the statistics that went into the pre-trained model itself you can you can use but now it's kind of focused on your specific context well one thing you can do costs relatively little it's maybe not the most common thing but something that can be worth looking at so this will still cost you computationally in terms of you have to run the entire large language model but you only have to tweak relatively small set of parameters that goes in before the big inference step so this we can do and the second thing that i introduced to you a little already last week which is called low rank adaptation or lora for for shorts and if you look at hugging face hub you'll find many of many of these so basically adaptations to to the big pre-trained models that can be used for any specific case that you like so you can also download these that have been fine students for so and so cases or on so and so data sets and it's kind of a it's kind of a beautiful thing so you have well so you know it a bit already but i'll show you also in the in the next slides again for a bit of repetition but you have the model with its weights in blue it comes with the pre-trained weights and these you just keep essentially so no messing about with the original model actually you just make your own kind of parallel parallel layers parallel mini network that goes sort of besides the the existing transformer blocks so when data comes in you actually just copy it send it through your little extra network in in orange and this is what you will train and then you merge your output with the output from the pre-trained model and then you sort of keep the existing knowledge so you don't run into this catastrophic forgetting thing but you kind of add your specific contexts or rather parameters through this approach so it's very cool definitely works okay if you're getting a little bit into into detail on

## Low-rank adaptation (LoRA) (16:00)

the size of these matrices it looks a bit like this so you start out with your input data and then yeah we looked at these matrix shapes so we'll not really we'll not really get into the details of matrix multiplication again this is this you can read on your spare time but we just want to look at the share sizes of these things and see if this is something that we can then manage to fit on on our hardware so we're starting out with n number of tokens so the sequence length multiplied by the dimensionality of my embedding space this i choose to be well typically we have three thousand four thousand something and the length of the sequence can be can be anything for the big models right now i'm not sure if i'm completely updated but can they many many hundreds thousands or millions of tokens even so these can be big matrices but okay this we have to live with the thing is right now that we just keep this weight matrix of the of my transformer block and then add you can see it visually these small nice little matrices in addition so we need two of them for the matrix multiplication to make sense so we want in the end to have something which is n times d so we're still having this dimensionality we have to put in but now i have a new number which is called r which i can choose myself so this r thingy basically governs how many new parameters do i want to add into this this big thing and i can say okay i have a pretty big computer so i'm going to choose r to be big or i can say i have a rather small computer so i'm going to choose this to be small so this then affects how much how much memory i need in my gpu to to do this training and then so i mentioned that the optimizer takes quite a bit of memory because that's to keep track of the of the history of my parameters now the number of parameters i need to keep track of is only these things in in reds so the big model in blue the pre-trained one still comes with a boatload of parameters right but i don't really touch them i don't really have to pay attention to them i need to train only this these smaller matrices and then sum it all up plus it up and then i have you can notice the dimensionality or the size of this matrix coming out is the same as the one that went in so basically the pre-trained stuff is untouched and the shapes that are very important they still match i just add in my my own stuff so to say so that's very convenient thing and we can see it adds then parameters which are yeah two times r times these because i have two of these and that's typically a lot less than this d squared unless i choose r to be extremely big for some reason but then i kind of and it doesn't really make that much sense anymore does it so the point is i can make this fairly small and then i can add all my yeah i basically learn stuff in this step this i adapt to my training data and then i keep still the pre-trained thing so i don't some less at risk for this catastrophic forgetting but i add what i need from from my data sets so a very yeah nice and appealing approach actually this one and does absolutely does absolutely work so this one could consider if you want to fine-tune your own network okay let's discuss something else but it's still on the same topic

## Quantization (20:06)

so yeah we still have limited resources as i mentioned and those the limitations will typically be memory because i have this truckload of parameters that has to go into memory and then you can say okay how much memory do i need for a single number typically well depending on what you do typically you're going to say that one number it requires 32 bits or maybe 64 bits and that is a very precise number and for many scientific calculations you want your numbers to be super precise but turns out okay in machine learning we can get away with actually reducing the number of bits we need for for one single number so we can dial down the numerical precision if we like in the in the weights and also the yeah activation of each layer it turns out okay if you yeah you have your network then you actually don't need to have a number which is i don't know 12 decimals you can get around with with a lot less maybe eight so instead of my 32 bit floats i can say i'm just going to cut it at 16 and then all of a sudden i have room for twice as many numbers so going to 16 bits doesn't really require much modification and if i have very particular cases so maybe i want to run some image recognition on a drone or something then i'm not going to have a data center data center available but i have an yeah what we call embedded system i have a small fpga or something then i can also squeeze it into integers if i like and use only eight bits for instance these are things or tricks that we can do to the numerical precision so going down to a very small number of bits which typically doesn't really make that much sense in in computation because we mess up the precision so much it still often works fine for for these deep learning approaches so eight bits is common four bits you can even use and yeah there are even networks there are basically just one bit so it's kind of on and off type of network or basically parameters of of the network and this is well how much can you do this it's a yeah it's a bit of a of a experimentation thing but this yeah it's becoming very popular so if you now go and look at hugging face and you want to download a model because you want to run it yourself locally for instance then it typically model name comes with all of these funny things appended to it so deep seek we know for instance this chinese thing which became very popular because it is super optimized you can download it in let's go through this so q4 for instance meaning quantized as we call it down to four bits of the parameters and then there are some other options as well which you can look up if you like which are also matters of yeah making more efficient memory efficient i should say versions of this model so typically if you want to run it on a decent computer that you have you're going to have to look into this because full model is kind of it really is reserved for data centers but you have a relatively spicy computer you can look at some of these and then also the compression ratio so if i have this quantized to eight bits for instance it's going to be still very good results but then but then only a quarter of the size which is which is pretty nice or i can go down to more stronger compression and then okay you're going to see that it it loses quality because we're losing numerical precision and then this translates into a worse computation of the probabilities of next token and so on so performance goes down but uh compression ratio goes goes up so if you want to do stuff on a smaller computer maybe even you need to run it on your phone or something uh then typically you're going to have to be looking at some of these quantized and optimized versions so maybe okay we can say it's a bit beyond the topics of the course but it's still something that you will if you want to run your large large language model locally you're going to meet this immediately so let's just mention it now well conversation and this optimization to just squeeze it into memory it's a yeah important thing but does lose a bit of of performance but often that's fine depends on your case but then let's look at let's look at one interesting thing so okay this quantization thing then we take the big model we train this huge model and then we just say that okay i'm going to cut the number of number of bits i'm just going to be a bit sloppy in in numerical precision but there's something else you can do which is which is kind of a fun thing

## Knowledge distillation (25:54)

which called knowledge distillation and it's basically trying to take a smaller model you skip this quantization business you just start from actually a smaller model and then you're going to say can i make this model mimic a big model and then you basically just you set up a dialogue with this too so you're doing a supervised training we're just saying the big model generates the answers so i can ask something and then i ask first the big model and it's going to say something and then i'm going to train the small model to basically parrot this thing you're just going to say the same as the big model so the big model is trained on all training data right but then the smaller model is really just trained on trying to replicate a big model and it sounds a bit a bit funny but it does absolutely work so you can do it something like this for classification and reasoning for instance i have some links to papers if you have if you want to read a bit extra on this but you kind of kind of see how it works right so instead of a human sitting down and making instructions and answers i'm just gonna say okay i'm going to optimize automate the entire thing and just have a large language model say the answers and a small large small small large language model it gets a bit funny but smaller model to basically replicate the same thing and the idea is kind of okay so the big model has learned as we say the parameters contain knowledge but we also know that it's often sort of redundant so we have billions of parameters right but typically we can do with we can do with less as long as the essence of all this knowledge is somehow retained in the parameters and the idea is to just then transfer the knowledge essentially to the to the smaller model through this yeah this procedure that goes through a bigger model and it does work and you can see for instance yeah do you find on on hugging face you'll find many examples but deep seek released many of these models so you have the the bigger ones but you'll see also that they have models which are distilled from different ones so we can take other open source model and then try to distill the knowledge into their own model which is then slightly smaller and then is possible to run on smaller hardware and this is the thing to the extent that claude you know claude writes or anthropic the parents company has sued some of these chinese AI providers for basically distilling what they have in the clone model into these chinese open source models so basically trying to to steal the knowledge from one model to another one by basically setting up a training loop where you send the question to claude get the answer and then this is going to be the instructions that you train your your own model for so you can okay you can kind of laugh a little bit about this because the training data for claude surely it's not entirely respecting copyright laws and so on okay if you're an industry big enough then you are the law in a sense but yeah well it's fairly recent i think so we'll see how how this goes but it's totally a thing so if you are very into this maybe you have heard about the minimax model which is very good for coding i have tried it myself and if i ask a simple question like who are you it doesn't say minimax it says i am claude so kind of obvious hints to where where some of the training comes from okay so this this shows that well yes knowledge distillation it definitely works and you can also get sued for doing it if you do it wrong okay that was all the advanced stuff on training tuning and adapting large language model

## Transformers for other data types (30:33)

for large language models for text which as well yeah of course when it's related to text we call it the large language model but then okay the name is kind of being misused for models that can do other things as well so if you ask if you upload an image to chat gpt it's very happy to describe it for you and i can also generate images based on your texts so by now we have what we usually call multimodal transformers so they can take any data in and give any data out not just text or not just images itself so we up until now have been working on a single data type at a time right but we'll see that we can in fact do a lot of things with these transformers i think okay we can do a little bit of intro on this before we move to the break and then continue with different transformer uses for the rest of the after 45 minutes we have we have left so okay i should say for this also it's not really that we're going to introduce very specific things that are very specific to the exam so now it's a bit more on just opening your eyes for the things that that can be done and also connecting it back to to one important concept basically of the course the big mantra of the course the pad

## General transformers (32:09)

and then you try to think about this then maybe if you're maybe if you're nice i will reveal it at the end okay so these transformers in general we use them to to process texts but text is just a sequence of of something and turns out we can use it for any sequence really and then in some cases we have to be a little bit creative on what we call a sequence and how we make a sequence but the important thing is that in general we made very few assumptions on what the input data will look like so remember that for convolutional networks we said that okay if pixels are neighbors then this neighboring relation is what we're going to look for or if we had time series then again neighboring time points is what we're going to look for but now we say okay the sequence is a sequence but i have this huge attention matrix and i look at the entirety of the sequence at once and words don't have to be neighbors in order to in fact or affect each other they can be anywhere and we do this nice trick of computing the big attention matrix and then dot products in abetting space and all this thing we this thing we looked at and we basically look at the entire thing at once which you cannot or typically would not do with convolutions then you just look at small sections each time right so we can use this for for basically anything we can take text fine like images videos was one of the things we looked at in the actually the first lecture when we had some different examples audio of course if we can do these other things we can probably do audio as well so we'll have a look at how this would work and then also the thing which we call point clouds which we didn't mention yet and we also don't really want to involve ourselves with that much in this course but if you have a self-driving car for instance it's typically going to have on the roof a kind of lidar thing or a radar or equivalent which basically looks around in in 3d and it measures the distances to do things and these should typically represent as a point cloud so one measurement is in that direction and so far away and these also work quite nicely for transformers so self-driving cars will typically represent the world as point clouds yeah but it doesn't really matter to us it's just data still we just need to be a little bit aware on how we pre-process it but as long as we can get anything of this stuff it into an embedding space then we are good to go so for some reason people are calling this different modalities these different input data types so yeah one definition then for today these are the different modalities and if you have a multimodal model then it can interact with all of these at once and we'll see then how you can actually okay i already said it but we'll see it visually how you can operate on all these at the same time and let's start with images

## Visual attention (35:36)

the last five minutes let's look at some images so we have the concept that we called attention and it sort of explains itself in a bit so which part of words no sorry of a sentence will you pay attention to and you can do the same reasoning with images as well so which part of an image uh would i focus my attention to when related to a certain question or yeah or a task really so one thing we can do is to implement the kind of similar kind of attention mechanism on images and then relate it to for instance caption generation so this is a model that is trained to do it's a transformer type model but it's trained to write this caption so image goes in caption comes out and then when we do this we can like i showed you before we can study the the values in the attention matrix and see not which word is unimportant but rather connect a word to an area of the image so if i say okay what is what is the or what are the attention scores for frisbee in relation to this image then actually the area of the frisbee lights up which is kind of obvious but also very cool to see that does in fact work so if i would look at if i would look at the scores for a woman that probably highlights this area where the where the woman is so the attention thing it was actually in in the air for a little while before we had the large language models but now that we have established the attention mechanism we can also apply it to images and to to other things so one more example so let's look at this one now again we have a transformer type model and we input an image and i didn't say yet how exactly we input the image but if we accept now that the image goes in you can probably see what this thing is and then we can go through the caption and see if looking at the attention scores actually explains what the model is doing so you know in a sense when you have a deep learning model with billions of parameters it's kind of hard to make an intuition about what exactly is the model doing it's you you know everything you know all the numbers but still it's a bit black box when it comes to deciding what actually the how the output relates to the inputs and so on but using the attention matrix then we can really explain a lot of these things which is nice so the caption for this so we put this image in and the caption that came out was a giraffe standing in a forest with trees in the backgrounds and now we have looked at the attention matrix then for all of these different words so let's see a giraffe for instance okay what is it focusing on let us focus on the giraffe that's nice if we are asking background what is it focusing on well then it's highlighting everything that is not the giraffe and trees so highlighting trees and in these words in between it's not really obvious to me what the model should be focusing on but at least this is what it this is what it does so in terms of explainable ai as we like to call it then really just plotting the attention matrix over on the data does give you quite a good insight in what the in what the model is is doing when making these these outputs so it does work kind of like expected so model again is not really it's not really obliged in any way to make sense to us but it's nice to see that it does in fact make some sense when we visualize like this so think then we can okay let's do the last thing before we take a break which is in some cases the model will do wrong the model gets some things wrong like for instance this caption as a guy playing a violin it seems and the output caption is a man wearing a hat and a hat on a skateboard yeah meaningless for so many reasons or this guy eating a sandwich seems and the model says man is talking on his cell phone apparently i don't think he is but you can essentially then go in and look at mistakes if you like to try to investigate where exactly did the did the model go wrong so if we highlight the scores for skateboards then apparently the violin is is being classified as a skateboard and maybe in a sense they have kind of similar shape and colors sometimes i don't know but this you're seeing the attention scores as highlighting basically can tell you where did the model go wrong so this guy eating a sandwich model thought the sandwich was a cell phone because this is what it's highlighting so this exact area of the image was the course so to say of the misprediction which is interesting in in itself so there's a lot of stuff you can do when it comes to investigating model outputs and finding out where exactly and also why which is even more important did it go wrong in in some cases so that was very quick on visual attention and i'm gonna show you then so now i kind of skipped the part of getting the image into tokens but we'll discuss this after the break so let's do that okay one minute over time so let's meet at 11 16 so we get the full 15 minutes break foreign foreign If that sounds right. I wrote in the comments that this is something I use in my classes, when I have a meeting to go through and show. Yes, and then maybe when we have a meeting we can fix it. Yes, actually. And I think I also fixed it so that it was in the right order, and then it's just a little bit of a challenge. But I can do it anyway. So if I had been able to add a little more to it, it would be fine. Yes, I agree. I feel like I have enough of my own. Yes, just a little more. Are you done with the system? Are you done with the system? That's it. Rewriting. It's much easier to get it. It's much easier to get it. Have you talked to RHO? RHO. It is a new, organized... It is a new, organized... It is a new, organized... It is a new, organized... It's a new, organized... A new, organized... She's got a new role. Wait a second. How did she get it? I don't know. I don't think she's older than me. I think she's older than me. Who is she? Emilie. I'm not sure if that's true. Yes, she's got lighter stripes, which are almost blue. Did she get it? Yes, because she just had breakfast at the hotel. When did you arrive at the hotel on Tuesday? I just arrived. I just assumed that you were with Jan. But now I see that you are... What does she do now? She's out running and training. I've been training with her for a while now. When she goes out running and what do you call it? What do you call it? High rocks. I don't know. I'm not sure if that's true. But it's like... I'm not sure if that's true. I'm not sure. But she's older than me. I agree with that. Emilie is more like... You just never let go of her. I think so. That's the most important thing. But I think it should be physical stress. Not like... It's more like you're pushing your body away. And then it's normal. It's normal. It's normal. Absolutely. Have you tested your eyes? It gives me energy. Because I've had... It's not like... It's not like... It's not like... I feel like there's something else. I feel like there's something else. I feel like there's something else. Yes, yes, yes. It's like... It's like... I've never experienced anything like that. I feel like... Okay, so we can start back up. So we saw... These attention scores, the attention matrix. It's very nice for visualizing what exactly is going on. And we can use this for text. And we can use it for images. And we can use it for anything. And it doesn't relate... Concepts that we can choose, essentially. So we can either relate different words in a sentence. We can relate parts of an image. We can relate the two. So for this to relate the words to a part of the image.

## Transformers for computer vision (57:33)

And that is cool. But then we kind of very... Easily skipped the part. Of asking how to actually define this token. So how do we define a unit of meaning when it comes to an image? So for text it was kind of fine. So each word has a meaning, as we know. And that is relatively easy. Just that in some cases there are different variants of words. So it makes sense to split them into smaller groups. And that would be then the subword tokens. Okay, so now we have one image. You can't really say that one single pixel contains very much meaning. Because you have to look at a group of pixels before you can actually detect something. Like we did with the convolutions. Well, the simplest thing is just to say. Okay, I say one pixel is a token. Fine. It doesn't really work that well. So the one thing is you have to then look at a lot of neighbors. This matrix is... Well, it's a square matrix, right? So it's quadratic and then number of tokens. Because we have the number of tokens on the row and the number of tokens in the columns. So it's going to be the number of tokens times number of tokens. So if you have a typical size image, it gets pretty big. Already for text, we kind of adapted to this by now. But when we started to do transformers for computer vision, this was a bit of a problem. So now it's okay, solvable. But still, it turns out that two of these common approaches that we have been using still work quite nice. So going to each individual pixel isn't really all that useful. Not that much to gain. But two things we can do. So step one, which is kind of first thing people are doing. Okay, let's cut the image up in maybe 50-ish different patches and try to tokenize this. Or second, as we have seen, you can do in so many cases, apply convolutions first and then find images while sort of down sampling at the same time.

## Vision transformer (ViT) for classification (60:00)

So what was then designed? We'll look at first when we came to this, the so-called vision transformer or VIT for short. These are in general architectures that do computer vision based on transformers somehow. But often if you talk about VITs, maybe it's this first type of model that was suggested that we mean. But still, the idea is the same for also newer types of models. Okay, so we had for an image, we know that there are these three dimensions. So it's the height of the image, like so, and the width, and also typically three color channels. But if you now switch this up and do kind of splits, then we have the number of patches. And then it's going to be height and width of this patch, okay. But then also the color channels, but the computation gets a little bit different. So we can finally then have a smaller number of tokens. So it's going to look a little bit like this. And then you can say, okay, seems a bit random, exactly how we cut this up and make patches. But we have at least patches that somehow retain a bit of meaning. So this one is generally just sky, this one is an air, that's a different air, and so on. So you can say that it seems a bit ad hoc, but still we have concepts being retained from the original image by doing this split into patches or super pixels, if you like. So we're going to do this exercise. And then for each patch, we still have an image and we have to do something. But then we can basically just stretch all the pixels out and flatten it into a vector. And if we have a vector, then we can make embeddings. So again, just transforming numbers in a sense, we take the pixel value here and there and there and there and just stretch it out in one long vector and then make embeddings for this. So then one such patch becomes essentially one token. And it would be pointing then in embedding space somehow, like this would be pointing to where sky should be. And this should be pointing in embedding space to where concept of air would live and so on. And some of them are maybe just fillers, but that's what we're also used with from texts. There are a lot of filler words also in texts. Now, once we have done this, then let's try to remember where exactly each patch is so we can do positional embeddings. And we also did this from text, right? Then we just took the position and embedded that and added up. And we can do the same thing. So this is going to be patch number one and patch number two and patch number three. And then this we also embed and add it all up. Oh, that's kind of, yeah, we took the image and then we did some tricks. And now it's a generic sequence and we can input it to the transformer. Now, it's going to look in the end a little bit like this. If we drop the boxes. So we had the tokens and then we flatten embeds and add the positional encoding. So it looks diagram looks suspiciously like what we had for this original attention paper, which is intentional, of course. And what we also can do, depending on what kind of task we want to solve, is to also add the class of this. Cat would also be something we could add in if we want to do prediction of classes or classification. And then, again, embed it and stuff it into the big transformer encoder block. So then it looks a lot like what we had for text. It's just that we have to do the process of making a sequence out of the image. But once we have done the embedding, then we're kind of in the big embedding space and then we don't really care anymore. Now it's everything is kind of the same. Everything is a vector in embedding space and we can do the attention mechanism unusual. And you can already here now see that. OK, as long as I can embed something, then everything can be solved with the transformer. So as long as I manage to pre-process my data somehow, that I can make vectors of it, then I can embed and then I am good to go.

## Positional embeddings (65:09)

OK, let's quickly discuss then again these positional embeddings. So there were a little bit for text. We had this slightly wonky approach of the original attention paper, which is to basically handcraft this and saying like the position is the sine or the cosine of the value and blah, blah, blah, which is it works, but it's maybe not obvious to us. And we kind of moved beyond this by now. So this option is still viable, but typically we do this learned embeddings. So I'll just say that these are the patches number so and so. Maybe best to classify in terms of patch and height coordinate and width coordinates. And then we can embed this. So used a lot this Keras dot layers dot embedding. And as long as I can put my numbers into this, then it works like any other embedding. So the funny thing is I can well, even though this is now a kind of two dimensional thing in height and width, it's still fine. So we can still extend this embedding layer very easily to take any type of dimensions or any number of dimensions in. So in case I maybe want to have like a three dimensional thing, which could be some medical cases, we have computer assisted tomography and we have MRI machines and these things which take an actual 3D image. Still, the embedding would work fine because it works. It extends easily to any number of dimensions. So again, we said it before embeddings are great also because they work for all sorts of data where I'm able to make a sequence out of it. And then we have vectors and then I'm in my lovely big abstract space and I can use transformers and I can transform and I can apply the context and learn all these things. So again, embeddings are cool and we can also combine this.

## Combined architecture (67:30)

So now we're getting into the phase that I will show a lot of fancy stuff and then you are supposed to reason by yourself that okay, if I can embed something, I can do anything. This transformer is the general architecture to allow me to solve any kind of any kind of task. But then I still have to, okay, I have to do some preprocessing to get the data in and I have to do some task specific stuff to get the data out or the prediction out I mean. But in the middle of this huge flowchart, which we'll see many examples of, it's all big abstract spaces and they are of some ridiculous dimensionality and they are lovely because they can do basically anything. So it's going to be very general, this space in the middle. And then, okay, we can also say the more general I am, the more training data I need to train such a thing because I have so few assumptions and that is, that is true. I'm going to need a whole lot of training data for these things. But then I can do like this example, okay, so a link to a paper, it's a little bit outdated, but it shows the principle of combined architectures quite nicely. So I want to do something where I find seagulls in an image, and maybe I do something like this. So then I combine the transformer with something else. So we already learned, we have nice architectures that can detect, sorry, detect correlations between neighboring pixels, that would be the convolutional networks. They work on time series and I can also do these recurrent networks that work nicely on sequences. And this can be my kind of step one feature extraction. So I need some features and usually just single pixels were a little bit unsatisfying to have as an approach, but I can do a CNN and then I have, remember, output of CNN is this dense layer and that is a sequence. And then hence, it works very nicely to put into a transformer. Okay, let's remember also these positional encodings. And then I have the big magic thing, it does many things. And in the end, I have several outputs that can predict different things. So I can say, okay, now I don't remember exactly why these have different colors, but at least this red one, it does find a target which has been trained for one seagull. The yellow one does the same thing, but these different so-called prediction heads, I can have multiple outputs. The green one didn't find anything and the blue one didn't find anything. So what did we have in the end? So now I'm kind of just glossing over these things, but we see as a concept, I can have a big transformer in the middle. And then I have my kind of data specific parts on the inputs, and I have my task specific parts on the outputs. And as long as I can formulate my tasks somehow, there can be any number of outputs and dimensionality and whatnot. As long as I can formulate it, I can typically solve with a transformer that does these transformations in my nice abstract vector space. And let's say, okay, I'm coming to it, but now because I have images, I will personally say that CNNs are nice. They work nicely on images, so I choose this, and then I make a little bit of an assumption, right? I assume that I have images and that they are following the patterns that images do, that neighboring pixels have a relation to another.

## Inductive biases (71:36)

And there we can say that we have something of an inductive bias when I set up my model. So it's basically the assumption that I make when I build my model. And some of these are kind of obvious. So if I have no deep learning at all, I have a plot that shows house prices as a function of square meters, something like this. Then you would expect that the bigger the house, the more expensive it is, and I can plot it as a straight line. And that is maybe a good idea. Maybe it's a bad idea. It's just my assumption that I go in with that this is going to be linear. It's going to be a straight line. And then I get a very simple model that has only two parameters. So the model is simple, but the assumptions are also strong. So if it turns out that, well, in fact, house prices are kind of a curvy thing, then my model is not very good, is it? It has poor predictive power. But the model was simple. So that's a trade off that I made. I'm going to say I assume this and then I can have a simpler model and then I'll need less training data and things get simpler in a sense. And this inductive bias business extends also to the other types of models that we have. So if I have convolutional neural networks, then I say that images are in a way that pixels or neighboring pixels make sense somehow. This is how we create patterns. And I'm also saying that an image can have a dog in the upper left corner or it can have a lower right corner and it's still going to be a dog. So I assume that we had these original things we mentioned that there's translational equivalence. So I can move stuff about in an image and it's still going to be an image of the same thing. So we made assumptions when we set up this CNNs for how an image is supposed to look. So we assumed something, right? And then you can do the same thing for these recurrent things that we used for time series. I'm assuming that each neighboring time points have an ordering and that like the end of a time series is more recent than the beginning of the time series, for instance. So that was, again, an assumption that I made or that you also made. This makes sense when we know that we have an ordering, right? But it's still biased in a sense. So it's not guaranteed that our assumptions make sense. And there we come to the transformers, which say that, well, OK, we assume something, which is that there is some kind of relation. Otherwise, things would be absolutely meaningless, wouldn't it? But that's about it. So assume there is some relation between my data points or my elements of data. But that's more or less about it. So it doesn't have to be ordered. It doesn't have to be a kind of linear shape or quadratic shape or exponential shape or any things. So it's very general purpose. And this is more or less why we see that transformers is kind of the go to architecture for solving the modality types of tasks. And also basically solving these big tasks that are where it's meaningful to have what we call the foundation model, a task or a model that can do many different things. So we assume very little. And that makes us very flexible. But with great flexibility comes a great need of training data. So we kind of let go. So we kind of knew already that to train a large language model, you need basically the entirety of the Internet. And if you want a very big vision transformer, you need a lot of typically millions or more of images and so on. So it's a kind of obvious trade off that if we want to be very general, then we're also going to be very little specific. So this is basically a choice for us. So if I know I want to solve one super specific task, maybe I don't need a transformer. But if I want to solve kind of everything, then a transformer is probably a good choice. So we come with some assumptions and some are good and some are not. And this is what we have to reflect upon whenever we are making deep learning models.

## Combining vision and text (76:33)

So let's stand still. I'm yetting about these models and how we can combine things. So we already looked at how we can do different tasks. So again, an example of different tasks that you can solve. If you have a vision language model, as we call it, when we can deal with both images and text. And if it's trying to solve different tasks. So now I have one model, but it's a transformer, a big thing, that can do different things. So what are typical questions you can ask to, for instance, chat GPT or your favorite service if you upload an image? You can ask like, OK, is there cats? Where is it? What type of cat? What's, yeah. Where basically is it in relation to other things? These are obviously text, right? But they relate to the image and then to get the different or to get answers of different kinds. We see that we are with one general model solving different specific tasks. So we're going to do some reasoning about one cat being in front of the other one, classifying segmentation. We know that we spent some time on description types of tasks. So again, it's one model, but I can do a lot of different things. So we're not then very specific to either classification or segmentation or sequence generation. We can, in fact, do a lot of stuff with one model. And here it's then up to you to try to realize how exactly can we achieve this given the stuff that I just showed you. But then there was one thing I wanted to show you, in fact. So let's you reason about this for 20 seconds while I set up a little example for you. Maybe it's running already.

## Audio transformers (78:45)

That's just this. Let's now skip images for a while. Still going to get back to the big insights towards the end, but let's look at audio as well. So typical things you want to do. Maybe you want to transcribe. You're at a boring meeting. Somebody tells you to take notes. Maybe I can just transcribe it directly by doing speech to text, something I would like. Or maybe I want to talk to Chachipity directly. I can do done text to speech so it can answer me or I can do back and forth. Same thing. Or, as it's also a fun thing, is to do text to music. To have a service where you say, OK, make me a country song with piano and banjo and stuff. There are different tools that will make pretty good sounding songs based just on your prompts. Just as we can generate images, we can generate sounds. Then there are two things we can do. Time series, we know how to operate on. We still have to preprocess it and somehow make these tokens, which we can embed. One thing is to look at just the time series itself. It is a bit difficult, we have to say, to operate on this since we have to deal with many different frequencies at a time. A popular trick is to convert waveforms into what we call spectrograms. Maybe you've seen this before, it's basically a plot that shows you the time on one axis and then the frequency on a different one. You can see that these things have more or less the same frequencies but distributed a little bit. This is some talking and you see it goes a little bit up and down in frequency. It's a popular thing. I don't remember what's the case now but the Spotify recommendations system was at least for a long time based on this. To actually just look at how the frequencies are distributed and then it can recommend you similar songs or opposite songs or the equivalent. There's also one tool which is very popular for transcribing speech to text which is called Whisper. It's an open AI thing but it's in fact open so it's given to us for free. We see that it looks very much like the original diagram for the original transformer. The original transformer has some stuff going on and then it has positional encodings and it has these transformer blocks and so on. We can see that in fact what goes in is a spectrogram. It's not the audio itself as a time series but it is a spectrogram and speech looks a bit like this. Then we have again this hybrid thing that we do convolutions. We can input it to the big great embedding space. This is where we can do a lot of different things and out comes sequences with text. And it looks a bit like, let me show you, stop this and then show you. So luckily it's open source this thing and that's good. So if I start this open AI tool it's called Whisper. It looks a bit like this and I can run it in real time if I like. Let me show you on both screens. There you can see how it works. Hopefully you can see. This is a small model so it makes some mistakes. So I say model and it's transcribed to bottle. But you can run of course a bigger model if you like. This is on my laptop so I don't have a very big model but it works pretty nicely. And this you can do yourself if you like. If you're in a meeting and you want to have everything spoken into text like so. Of course there are different tools that will do this for you but it's a little bit more fun to do it yourself, isn't it? So look it up, it's called Whisper and you can run this on your laptop as much as you like. It is a transformer type but it comes in different sizes. The biggest one I can't fit on this laptop but I'm one of the smaller ones. It runs just fine and I'm running Zoom at the same time and still it works. So pretty cool. Okay, that was Whisper. Let's go back. So we're actually supposed to discuss how this works. And now we're getting into one nifty trick of the attention mechanism. So of course you all read the book so you know this. But in the example that we showed when we started on transformers and the attention mechanism was that I showed you what we call self-attention. So I have one sentence on the columns and I have the same sentence on the rows. And then we try to figure out how the different words affected each other so that when we read station I could use the presence of the word train to indicate that this is a train station. But then as you read in the book of course we can also do translation and then I have different inputs and outputs. Then I want the English for instance going in and I want the maybe French going out. And then I can look at the attention scores between the French words, no sorry the English words and the French words and see how this works. And then we didn't have self-attention. But then we have the thing which is a more general often called cross-attention which we need for the sequence to sequence thing. So notice now it looks a little bit different so we don't have only the encoder or the decoder block. But we do in fact if we follow the diagrams it's a bit tricky with these diagrams I'll give you this. But you can basically see that we try to compute the attention between different blocks and the idea is to relate them the sequence inputs to the sequence outputs. So now I'm in fact computing attention between tokenized words, tokenized speech I mean into text words instead. And this works fine because I have the transformer as a super general tool. So once everything lives in the big embedding space then I don't care where it came from in terms of data types. So maybe it was text, maybe it was speech, maybe it was an image. As long as I can tokenize it I don't care anymore. And then I can deal with any types of inputs and outputs data and then doing this transition from one data type to a different one. It works really quite well as we even saw in this little demo. Which more things can we do? Well we can do speech to text but I can do speech synthesis for instance which is kind of a funny thing. I didn't set up an example for this but probably you saw it was popular maybe two years ago that you could record a little snippet of somebody speaking maybe yourself. And then you can input texts and then have the text being read up in your accent and tone. Which is a nice way of course to start generating fake stuff. You can do it with videos. You can imagine that you can basically generate fake videos with fake speech and having celebrities say weird stuff. It's not really that difficult to do. If you know how the transformer works then we have some input text. And then we had the big embedding space where you based on your acoustic prompts can transform around the embedding space and then put it in a corner of space where the audio once you decode it would follow the same accent and tone when it comes out. Which shows again we can deal with any data types. And the big thing was in this big embedding space this big word cloud that we showed some time back was that everything is about context really and then transforming between transforming these token vectors in one embedding space to the next where meaning is retained but it's affected by context. So if I say that my context is my voice then I can have speech sentences with my voice or I can say that the context is some different person's voice and I can read up the same with a different person's voice tone and accent for instance. So a lot of fun and of course you can ask what should we use it for except generating fake things. I don't know. But it's kind of cool still. So finally so we're going to end hopefully 10 minutes early because I have to run to a meeting again. People like these meetings all the time. But one final thing which is also completely useless but you can of course play games. You can play Starcraft 2 if you like with the transformer.

## The Challenge of StarCraft (89:15)

And unfortunately since I'm showing you this over Zoom it's going to be a bit choppy but have a look at the video yourself if you like. So can you play games with transformers? Yes you absolutely can. It's just one example. So again the quality is of course better on the video if you look at it yourself. This is just because I'm showing it over Zoom. But transformer models they can play games they can do anything which really just shows that all sorts of data that I can tokenize. I can operate on to solve different tasks. So in this case my task is to make these different units work different places. But still as long as I can embed the entire thing then I can also solve this task which is I would say still kind of fun.

## Multimodal transformers (90:18)

So that's kind of it for now. So we went a little bit beyond Pensum but we still have the big insight for you to think about which is that what are we doing really. So we said in the beginning that we want to do pattern recognition and stuff. And yes this is what we're doing but we just elevated it to the most general sense by now. So for the rest of the weeks after Easter we will try to generate some pictures and stuff which is also fun. But it's still going to be the same concept that we take whatever we have of input data. And then we somehow tokenize it, put it into a big latent space and this is where we can operate on it. We can transform, we can massage it around and we can give it context based on our prompts. And then we can basically solve any kind of task in any type of output data format as long as we're able to basically formulate the task. So we're now at the most general stage that we have in deep learning as of now. Typically we see that as research advances we will find different things. But this is kind of the most general state that we know of now. So as long as I can tokenize and embed my data then this big concept of doing stuff in latent spaces still applies. And we saw that while we're drawing the parallels this was in fact what we did all the way in beginning with convolutions as well. We extracted patterns and then we took the output that was now a new, technically a big vector space where we were doing again pattern recognition. So this business of going from the input data to latent spaces where we try to extract knowledge and then to the final output which is the solution to this task. This we have in fact been doing all the time although it was kind of a little bit hidden in the details you could say. So I think we're going to end it there and then spend the next Easter week thinking very hard about the concepts of deep learning and then also working on the projects. So thank you can show you just quickly the plan again. So since after Easter the Monday is off and the Tuesday I think we just do project work so that you have a lot of time to make the best possible projects. And then we'll do the generative bits on images and I will maybe switch the weeks around a little bit. So it could be useful for your project when you develop web app that I show you some more stuff on deployments and ML operations as it's called. But I think also you have the basics so maybe we don't need this. I will decide over Easter. Then April 30 is the big date right. That's about it for now I think. So let's end it there and head to lunch and then we meet again after Easter. And after that project week as well so week 16 we'll see each other again.
